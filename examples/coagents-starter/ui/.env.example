# OPENAI_API_KEY is NOT needed in the frontend - the backend handles all AI via ExperimentalEmptyAdapter
# Only uncomment if you switch to an AI service adapter (e.g., OpenAIAdapter) for Copilot Suggestions
# OPENAI_API_KEY=your-openai-api-key-here

# Backend agent server port
AGENT_PORT=8020
REMOTE_ACTION_URL=http://localhost:8020/copilotkit

# Frontend UI port
UI_PORT=3015

# PostgreSQL connection for coagent_starter (optional - only needed for persistent storage)
# DATABASE_URL=postgresql://username:password@localhost:5432/dbname
