node_modules/
.vscode/
.idea/
.env
.DS_Store

.next
# The E2E LOCK_SKIN project's isolated build dir (NEXT_DIST_DIR in
# playwright.config.ts) — two dev servers cannot share one .next.
.next-locked

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Playwright
test-results/
playwright-report/
playwright/.cache/
