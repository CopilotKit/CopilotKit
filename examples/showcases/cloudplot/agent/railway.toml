[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["/examples/showcases/cloudplot/agent/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
