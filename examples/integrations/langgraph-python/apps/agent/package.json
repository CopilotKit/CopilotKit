{
  "name": "@repo/agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run langgraph dev --port 8123 --no-browser",
    "dev:js": "npx @langchain/langgraph-cli dev --port 8123 --no-browser",
    "postinstall": "uv sync"
  },
  "devDependencies": {
    "@langchain/langgraph-cli": "^1.1.12"
  }
}