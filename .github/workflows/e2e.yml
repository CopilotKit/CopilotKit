name: End to End Tests

on:
  workflow_dispatch:
    inputs:
      sha:
        description: "sha"
        required: true
      ref:
        description: "ref"
        required: true
      urls:
        description: "urls"
        required: true
      source-workflow-run-id:
        description: "source-workflow-run-id"
        required: true

jobs:
  e2e_tests:
    name: E2E Tests
    runs-on: ubuntu-latest
    permissions:
      checks: write
      contents: read
      pull-requests: write
      statuses: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}

      # - name: Download artifact
      #   uses: actions/download-artifact@v4
      #   with:
      #     name: tests-env-file
      #     path: tests/
      #     github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Status Check
        uses: actions/github-script@v6
        with:
          script: |
            await github.rest.checks.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              name: 'E2E Tests',
              head_sha: '${{ inputs.sha }}',
              status: 'in_progress',
              started_at: new Date().toISOString()
            })

      - name: Print Inputs
        run: |
          echo "Inputs: ${{ toJson(inputs) }}"

      - name: Download Artifact
        uses: actions/download-artifact@v4
        with:
          name: e2e-urls
          run-id: ${{ inputs.source-workflow-run-id }}

      - name: Print dummy.txt
        run: |
          cat ./dummy.txt

      # - name: Print URLs (default)
      #   run: |
      #     echo "URLs: ${{ inputs.urls }}"

      # - name: Print URLs (base64 decoded)
      #   run: |
      #     echo "echo ${{ inputs.urls }} | base64 -d"

      # - name: Run Tests
      #   id: run_tests
      #   run: |
      #     cat tests/.env.test
      #     echo "Running E2E tests..."
      #     # Your actual test commands here

      - name: Wait 1 minute
        run: sleep 60
          
      # - name: Update Status Check
      #   if: always()
      #   uses: actions/github-script@v6
      #   with:
      #     script: |
      #       const conclusion = '${{ steps.run_tests.outcome }}' === 'success' ? 'success' : 'failure';
      #       await github.rest.checks.create({
      #         owner: context.repo.owner,
      #         repo: context.repo.repo,
      #         name: 'E2E Tests',
      #         head_sha: '${{ github.event.client_payload.sha }}',
      #         status: 'completed',
      #         conclusion: conclusion,
      #         completed_at: new Date().toISOString(),
      #         output: {
      #           title: conclusion === 'success' ? 'E2E Tests Passed' : 'E2E Tests Failed',
      #           summary: conclusion === 'success' ? 'All tests passed successfully' : 'Some tests failed'
      #         }
      #       });
      
