name: test / integration / docs
on:
  pull_request:
    branches: [main]
    paths:
      - "showcase/shell-docs/src/content/**"
      - "showcase/shell-docs/model-allowlist.json"
      - "scripts/validate-doc-model-names.ts"
      - "scripts/doc-tests/**"
      - ".github/workflows/test_integration-docs.yml"
  push:
    branches: [main]
    paths:
      - "showcase/shell-docs/src/content/**"
      - "showcase/shell-docs/model-allowlist.json"
      - "scripts/validate-doc-model-names.ts"
      - "scripts/doc-tests/**"
      - ".github/workflows/test_integration-docs.yml"

env:
  # Telemetry off for automated runs (OSS-565). CI jobs that boot real apps
  # emit `oss.runtime.*` / `oss.inspector.*` events that are indistinguishable
  # from user activity in PostHog. These cover everything that reads the
  # environment, i.e. the runtime and its `oss.runtime.*` events.
  #
  # Browser-side Inspector events are NOT covered by these vars: they would only
  # reach the browser through the async /info handshake, which an early
  # interaction can beat. The Inspector suppresses itself on
  # `navigator.webdriver` instead, and the "Block telemetry egress" step below
  # is the job-wide backstop.
  COPILOTKIT_TELEMETRY_DISABLED: "true"
  DO_NOT_TRACK: "1"

# Least-privilege by default. Individual jobs/steps can widen when needed.
permissions:
  contents: read

jobs:
  validate-model-names:
    runs-on: depot-ubuntu-24.04-4
    timeout-minutes: 15
    permissions:
      contents: read
      # id-token: write is required for Depot OIDC auth (runs-on: depot-ubuntu-*).
      id-token: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm tsx scripts/validate-doc-model-names.ts

  doc-tests:
    runs-on: depot-ubuntu-24.04-4
    timeout-minutes: 15
    needs: validate-model-names
    permissions:
      contents: read
      # id-token: write is required for Depot OIDC auth (runs-on: depot-ubuntu-*).
      id-token: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - name: Block telemetry egress
        # Backstop for the env-var and navigator.webdriver opt-outs (OSS-565):
        # nothing in this job can reach the telemetry sink, including processes
        # that never inherit the workflow env.
        run: bash .github/scripts/block-telemetry-egress.sh

      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: pnpm
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - run: pnpm install --frozen-lockfile
      - name: Start aimock
        run: |
          # aimock is pinned as a workspace dependency (@copilotkit/showcase-scripts)
          # and installed from the frozen lockfile above — no ad-hoc `npm install -g`.
          # The `llmock` bin is aimock's fixtures-based CLI (the package also ships an
          # `aimock` bin, which is the newer config-only CLI that does NOT accept
          # --fixtures). Invoke the workspace-installed bin directly from the repo
          # root so the root-relative --fixtures path resolves correctly (a
          # `pnpm --filter exec` would run inside showcase/scripts and break the path).
          nohup ./showcase/scripts/node_modules/.bin/llmock --fixtures scripts/doc-tests/fixtures --validate-on-load > /tmp/aimock.log 2>&1 &
          for i in $(seq 1 60); do
            if curl -sf http://localhost:4010/health; then
              echo "aimock ready"
              exit 0
            fi
            sleep 1
          done
          echo "aimock failed to start. Logs:"
          cat /tmp/aimock.log
          exit 1
      - run: pnpm tsx scripts/doc-tests/extract.ts
      - run: pnpm tsx scripts/doc-tests/run.ts
