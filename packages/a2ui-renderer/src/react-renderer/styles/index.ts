/**
 * v0.9: Styles are now handled by @a2ui/react internally.
 * These functions are kept as no-ops for backward compatibility.
 */

export function injectStyles(): void {
  // v0.9: styles are injected by @a2ui/react/v0_9 components automatically
}

export function removeStyles(): void {
  // v0.9: no-op
}
