/**
 * Pluggable conversation-history store interface.
 * Full implementation is provided in a later task.
 */
export interface HistoryStore {
  // Populated in a later task.
  [key: string]: unknown;
}
