"""
Human in the Loop demo — uses the shared MS Agent Framework agent at src/agents/agent.py

The go_to_moon tool uses approval_mode="always_require" to trigger HITL confirmation.
All demos share the same agent instance served by agent_server.py.
"""
