<Steps>
  <Step>
    ### Install the CopilotKit SDK

    <InstallSDKSnippet />

  </Step>
  <Step>
    ### Emit state snapshots from tool results

    Built-in Agent reads the current state from the runtime input and emits
    fresh state snapshots after server tools complete. This supports shared
    state read/write demos, but not LangGraph-style per-token state streaming.

    <DemoCode file="src/lib/factory/tanstack-factory.ts" region="built-in-agent-state-bridge" />

  </Step>
</Steps>
