<Steps>
  <Step>
    ### Install the CopilotKit SDK

    <InstallSDKSnippet />

  </Step>
  <Step>
    ### Register the in-process agent

    `useAgentContext` sends read-only app context through the same runtime
    endpoint as chat messages. No framework middleware is required; the
    Built-in Agent receives that context as part of the run input.

    <DemoCode file="src/app/api/copilotkit/route.ts" region="built-in-agent-runtime" />

  </Step>
</Steps>
