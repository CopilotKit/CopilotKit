"""Hermes AG-UI adapter.

Exposes the Hermes ``AIAgent`` over the AG-UI protocol (HTTP/SSE), mirroring
the structure of ``acp_adapter`` but targeting AG-UI event streams and the
client-side ("frontend") tool execution pattern.
"""
