node_modules/
.next/
.env.local
.env
dist/
playwright-report/
test-results/

# Shared modules (copied by CI)
shared_frontend/
shared_python/
shared_typescript/

# The vendored clawg-ui plugin ships its built dist in the image, so it must
# stay tracked despite the blanket dist/ ignore above.
!gateway/clawg-ui/dist/
!gateway/clawg-ui/dist/**
