# Reasoning (Default Render)

Same backend as `reasoning-custom`, but the page passes NO custom
`reasoningMessage` slot — CopilotKit's built-in
`CopilotChatReasoningMessage` renders the reasoning as a collapsible
"Thought for X" card.
