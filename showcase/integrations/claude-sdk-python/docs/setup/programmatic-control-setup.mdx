<Steps>
  <Step>
    ### Run Claude through an AG-UI endpoint

    Programmatic control starts from the same AG-UI run boundary as the chat UI.
    Wrap Claude Agent SDK once, then use the complete frontend example below
    to add a user message with `useAgent` and dispatch or stop the run with
    `useCopilotKit`.
  </Step>
</Steps>
