"""
Agent implementation for Headless Interrupt (testing).

Shares ``src/agents/src/interrupt_agent.py`` with the in-chat
``gen-ui-interrupt`` demo. The frontend page resolves the langgraph
``interrupt()`` payload from a plain button grid via ``useHeadlessInterrupt``
rather than ``useInterrupt``'s built-in in-chat render prop.
"""
