"""
Agent implementation for Headless Chat (Complete)

The real graph lives at `src/agents/src/headless_complete.py` and is
registered in `langgraph.json` under the `headless_complete` id. This
placeholder exists so the per-demo folder matches the canonical layout
used by the other fastapi demos.
"""
