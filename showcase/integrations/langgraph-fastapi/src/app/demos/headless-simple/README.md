# Headless Chat (Simple)

Minimal custom chat surface built on `useAgent`. Demonstrates how to
compose a chat UI from primitives without the pre-built `<CopilotChat />`
component.

See the LangGraph Python variant for the canonical reference.
