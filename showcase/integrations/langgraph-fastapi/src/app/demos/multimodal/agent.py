"""
Agent implementation for Multimodal Attachments

The real graph lives at `src/agents/src/multimodal_agent.py` and is
registered in `langgraph.json` under the `multimodal` id. This
placeholder exists so the per-demo folder matches the canonical layout
used by the other fastapi demos.
"""
