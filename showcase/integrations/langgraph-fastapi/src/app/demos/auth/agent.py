"""
Agent implementation for Authentication

This demo reuses the main `sample_agent` graph — there is no dedicated
graph. The auth gate is enforced at the runtime layer via an onRequest
hook (see `src/app/api/copilotkit-auth/route.ts`).
"""
