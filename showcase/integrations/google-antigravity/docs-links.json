{
  "framework": "google-antigravity",
  "features": {},
  "missing": [
    {
      "feature": "all",
      "reason": "No antigravity-scoped docs tree exists on docs.copilotkit.ai — google-antigravity is born-in-showcase and the adapter is still on an unmerged AG-UI branch (PR #2277), so there is no /antigravity/... namespace to point at. The previous version of this file pointed every og_docs_url at /adk/... pages, which document Google ADK, not this adapter. Following langroid (the other born-in-showcase package with no docs namespace), features is left empty so every demo falls through to the framework-agnostic feature-level og_docs_url + shell_docs_path in showcase/shared/feature-registry.json. Demos with no registry-level entry render red — a deliberate honest signal that no documentation page exists yet, not an oversight. Replace with per-feature entries once an antigravity docs namespace ships."
    }
  ]
}
