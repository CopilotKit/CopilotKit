# Exact, validated dependency lock for the google-adk showcase.
# Ag-UI ADK keeps the custom `google-adk` floor at >=1.28.1, so the 1.x line
# (never a major bump the adapter has not been validated against) is pinned.
fastapi==0.141.1
uvicorn[standard]==0.53.0
python-dotenv==1.2.3
pydantic==2.13.5
google-adk==1.39.1
google-genai==2.24.0
ag-ui-adk==0.7.0
ag-ui-protocol==0.1.18