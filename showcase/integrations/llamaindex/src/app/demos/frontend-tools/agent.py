# Agent implementation for Frontend Tools
# The LlamaIndex agent is defined in src/agents/agent.py
# The frontend_tools agent name is registered in src/app/api/copilotkit/route.ts
# and resolves to the shared AG-UI workflow router.
