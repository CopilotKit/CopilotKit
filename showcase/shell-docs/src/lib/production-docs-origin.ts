/**
 * The canonical docs origin. Kept apart from `runtime-config.ts`, which is
 * server-only, so client components can name it too.
 */
export const PRODUCTION_DOCS_ORIGIN = "https://docs.copilotkit.ai";
