---
title: Introduction
icon: "lucide/BrainCircuit"
description: Bring your Deep Agents to your users with CopilotKit via AG-UI.
hideHeader: true
# The landing page carries its own prompt button in the hero; the page-tools
# row above the title would put a second one on the page, which the data-record
# partner pages do not have.
hidePageActions: true
---

<FrameworkOverview
  frameworkName="Deep Agents"
  frameworkIcon={<DeepAgentsIcon className="h-12 w-12" />}
  header="Build interactive apps with Deep Agents"
  subheader="CopilotKit connects your Deep Agents agents to chat, custom UI, and human input."
  bannerVideo="https://cdn.copilotkit.ai/docs/copilotkit/videos/coagents/overview.mp4"
  architectureImage="https://cdn.copilotkit.ai/docs/copilotkit/images/generic-agui-architecture.png"
  tutorialLink="/langgraph/tutorials/ai-travel-app"
  guideLink="/deepagents/quickstart"
  initCommand="npx copilotkit@latest init"
  lede="Bring your Deep Agents agent into your product."
  supportedFeatures={[
    {
      title: "Generative UI",
      videoUrl: "https://cdn.copilotkit.ai/docs/copilotkit/videos/coagents/haiku.mp4",
      iconKey: "paintbrush",
      description: "Show subagent progress and outputs in your app.",
      documentationLink: "/deepagents/generative-ui"
    },
    {
      title: "Human-in-the-loop",
      videoUrl: "https://cdn.copilotkit.ai/docs/copilotkit/images/coagents/human-in-the-loop-example.mp4",
      iconKey: "user",
      description: "Let users respond when a graph interrupts.",
      documentationLink: "/deepagents/human-in-the-loop"
    },
    {
      title: "Shared state",
      videoUrl: "https://cdn.copilotkit.ai/docs/copilotkit/videos/coagents/shared-state.mp4",
      iconKey: "repeat",
      description: "Share graph state with your interface.",
      documentationLink: "/deepagents/shared-state"
    }
  ]}
  capabilitiesFootnote={{
    text: "Chat surfaces, headless UI, frontend tools and subagent flows work with Deep Agents too.",
    linkLabel: "And more",
    href: "/deepagents/build-with-agents"
  }}
  connect={{
    intro:
      "Your agent stays where it runs today, on LangGraph Platform or your own service. CopilotKit reaches it over AG-UI, so nothing inside the agent changes.",
    filename: "app/api/copilotkit/route.ts",
    language: "ts",
    guideLink: "/deepagents/quickstart"
  }}
>

```ts title="app/api/copilotkit/route.ts"
import { CopilotRuntime, createCopilotRuntimeHandler } from "@copilotkit/runtime/v2";
import { LangGraphAgent } from "@copilotkit/runtime/langgraph";

const runtime = new CopilotRuntime({
  agents: {
    sample_agent: new LangGraphAgent({
      deploymentUrl: process.env.LANGGRAPH_DEPLOYMENT_URL!,
      graphId: "sample_agent",
      langsmithApiKey: process.env.LANGSMITH_API_KEY!,
    }),
  },
});

const handler = createCopilotRuntimeHandler({
  runtime,
  basePath: "/api/copilotkit",
  mode: "single-route",
});

export const POST = handler;
```

</FrameworkOverview>
