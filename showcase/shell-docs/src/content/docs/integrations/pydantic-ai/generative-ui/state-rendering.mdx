---
title: "State Rendering"
icon: "lucide/Bot"
description: Render agent state with custom UI components.
---

## Showcase availability

<Callout type="warn" title="This Showcase example is not ready">
  The Pydantic AI state-rendering route does not yet have a matching state
  publisher in its selected Showcase backend. Its frontend expects `steps`,
  while the backend currently exposes `todos`, so this route cannot demonstrate
  a working state-rendering contract.
</Callout>

This is a gap in the Pydantic AI Showcase integration, not a limitation of
Pydantic AI. Until the backend publishes the state that this view consumes, do
not copy the previous example from this page.

## Use shared state today

For a working Pydantic AI state synchronization example, follow the
[Shared State guide](/pydantic-ai/shared-state). It shows the integration's
current two-way state flow, including how the UI reads and updates the state
the selected backend publishes.

## What this example needs

To make this state-rendering route runnable, the Showcase backend needs to
publish the `steps` state expected by the selected frontend, and the route must
target that backend. The guide will show the actual frontend and backend source
once that implementation exists.
