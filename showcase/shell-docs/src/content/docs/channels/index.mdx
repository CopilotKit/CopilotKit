---
title: Channels
nav_title: Overview
icon: "lucide/MessagesSquare"
description: Run one AG-UI agent in Slack or Microsoft Teams through managed CopilotKit Intelligence connections.
doc_type: explanation
---

Channels brings your agent into the conversations where work already happens.
Your agent still runs in your infrastructure. CopilotKit Intelligence owns the
Slack or Microsoft Teams connection, delivers each turn to your Channels SDK
listener, and sends the response back as native channel UI.

<Image
  src="/images/slack-bot-generative-ui-light.png"
  alt="A Slack user asks the Kite agent to chart project progress, and Kite responds with a native chart."
  width={920}
  height={822}
  className="block dark:hidden my-8 w-full max-w-3xl mx-auto rounded-xl"
/>
<Image
  src="/images/slack-bot-generative-ui-dark.png"
  alt="A Slack user asks the Kite agent to chart project progress, and Kite responds with a native chart."
  width={1024}
  height={915}
  className="hidden dark:block my-8 w-full max-w-3xl mx-auto rounded-xl"
/>

<Cards>
  <Card
    title="Build for Slack"
    href="/slack"
    description="Configure a managed Slack app, connect your agent framework, then add tools, approvals, and durable state."
  />
  <Card
    title="Build for Microsoft Teams"
    href="/teams"
    description="Configure an Azure Bot and Teams app, connect your agent framework, then add tools, approvals, and durable state."
  />
</Cards>

Discord and WhatsApp support is coming soon.

## How a message reaches your agent

{/* Publication gate: insert Mike's final Channels architecture diagram here. Do not replace it with a temporary diagram. */}

1. A person messages your app in Slack or Teams.
2. CopilotKit Intelligence receives the platform event using the credentials
   configured for that Channel.
3. A persistent Intelligence gateway connection delivers the turn to your
   long-running Channels SDK process.
4. The SDK runs your agent over [AG-UI](/agentic-protocols/ag-ui), executes
   channel tools, and renders the response.
5. Intelligence sends the result back as native Slack Block Kit or Teams
   Adaptive Card content.

This split keeps platform credentials out of your agent process without moving
your agent, tools, or business logic into CopilotKit.

| You run                                | CopilotKit Intelligence manages               |
| -------------------------------------- | --------------------------------------------- |
| The agent and its model credentials    | Slack and Teams platform credentials          |
| A long-running Channels SDK listener   | Platform ingress and credentialed delivery    |
| Tools and approval business logic      | Runtime registration, health, and reconnects  |
| Deployment, logs, and application auth | Conversation-history delivery to the listener |

## Match your Channels configuration

Keep these separate:

- **Display name** is the human-readable name in Intelligence.
- **Code** is the project-unique lowercase identifier. It must exactly match
  `createChannel({ name })`.
- **Agent connection/backend** is what `agent` creates for each thread. Remote
  AG-UI backends need a URL; in-process frameworks such as Built-in Agent do
  not.

The agent framework is independent of the channel. Use the agent selector in a
[Slack](/slack) or [Teams](/teams) quickstart to see the setup for Built-in
Agent, LangGraph, CrewAI, Mastra, Pydantic AI, or another supported backend.
