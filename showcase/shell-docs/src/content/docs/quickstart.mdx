---
title: Quickstart
description: Get started with CopilotKit — pick your framework to see the full quickstart guide.
icon: "lucide/Play"
hideTOC: true
---

{/* This file exists so the sidebar's "Quickstart" entry has a backing
   page (buildNavTree skips entries without one). The real per-framework
   quickstart guides live at /<framework>/quickstart. SidebarLink
   prepends the user's stored framework when present, so clicks from
   anywhere in the docs land on the right framework's guide. The few
   visitors who do hit /quickstart directly are 308-redirected to / by
   next.config.ts, where the merged docs landing handles the picker
   experience. */}

This page is a routing shim. Pick your framework from the sidebar selector or [the docs landing](/) to see the full quickstart for your stack.
