---
data: sdk-pair.json
---

```bash title="Terminal"
npm install --save-exact @copilotkit/channels@{{channels}} @copilotkit/runtime@{{runtime}}
```

These two versions are the tested pair. Upgrade them together, never one
without the other: mismatched versions can install two copies of
`@copilotkit/channels-core`, which makes `tsc` fail.
