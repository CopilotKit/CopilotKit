---
title: "Markdown"
description: "Explicit markdown text block in a bot message."
---

## Overview

`Markdown` is an explicit markdown text block. It behaves like [`Section`](/reference/channels/components/Section) but signals intent: this content *is* markdown (e.g. agent output you're passing through), not incidental body text.

## Import

```tsx
import { Markdown } from "@copilotkit/channels-ui";
```

## Props

<PropertyReference name="children" type="BotChildren">
  The markdown source.
</PropertyReference>

## Usage

```tsx
<Message>
  <Markdown>{agentSummary}</Markdown>
</Message>
```

## On Slack

Renders as a `section` block whose text is passed through the adapter's Markdown → mrkdwn translation, truncated at **3000 characters** (`SLACK_LIMITS.sectionText`).

## Related

- [Section](/reference/channels/components/Section) — general body text
- [markdownToMrkdwn](/reference/channels/slack/markdownToMrkdwn) — the Markdown → mrkdwn translation
