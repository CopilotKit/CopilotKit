---
title: "renderComponents"
description: "Translate the bot-ui BotNode IR to Discord Components V2, degrading within Discord's per-element budgets (DISCORD_LIMITS)."
---

## Overview

`renderComponents` translates the [BotNode IR](/reference/bot/types/BotNode) into a single top-level Components V2 `ContainerBuilder`; `renderDiscordMessage` additionally wraps it in the `{ components, flags }` shape required by `channel.send` / `message.edit`, with `MessageFlags.IsComponentsV2` set. The adapter calls these for you on every [`thread.post`](/reference/bot/classes/Thread) — reach for them directly when testing components or building custom egress.

## Signature

```ts
import { renderComponents, renderDiscordMessage, DISCORD_LIMITS } from "@copilotkit/bot-discord";

function renderComponents(ir: BotNode[]): ContainerBuilder;

function renderDiscordMessage(ir: BotNode[]): {
  components: ContainerBuilder[];
  flags: number; // MessageFlags.IsComponentsV2
};
```

## Component mapping

| Component | Components V2 |
|-----------|---------------|
| [`Message`](/reference/bot/components/Message) | outer `ContainerBuilder` (accent color if `<Message accent="#hex">`) |
| [`Header`](/reference/bot/components/Header) | `TextDisplayBuilder` with `# ` prefix |
| [`Section`](/reference/bot/components/Section) / [`Markdown`](/reference/bot/components/Markdown) / [`Text`](/reference/bot/components/Text) | `TextDisplayBuilder` (Markdown rendered via `discordMarkdown`) |
| [`Fields`](/reference/bot/components/Fields) | `TextDisplayBuilder` with bold-label lines (`**label** value`) |
| [`Context`](/reference/bot/components/Context) | `TextDisplayBuilder` with `-# ` subtext lines |
| [`Actions`](/reference/bot/components/Actions) | one or more `ActionRowBuilder` (buttons chunked ≤5/row, selects each get their own row) |
| [`Button`](/reference/bot/components/Button) | `ButtonBuilder` inside an `ActionRowBuilder` |
| [`Select`](/reference/bot/components/Select) | `StringSelectMenuBuilder` inside its own `ActionRowBuilder` |
| [`Image`](/reference/bot/components/Image) | `MediaGalleryBuilder` with one `MediaGalleryItemBuilder` |
| [`Divider`](/reference/bot/components/Divider) | `SeparatorBuilder` (small spacing, divider line) |
| [`Table`](/reference/bot/components/Table) | `TextDisplayBuilder` with a reconstructed GFM pipe table fenced by `discordMarkdown` |
| [`Input`](/reference/bot/components/Input) | skipped — modal-only on Discord; a warning is logged |

## IsComponentsV2 flag

`renderDiscordMessage` always sets `MessageFlags.IsComponentsV2` on the returned `flags`. Discord requires this flag when the message body contains a top-level `ContainerBuilder`; without it the API rejects the payload.

## Per-element budgets

Discord caps every element. The renderer **degrades instead of failing**: over-long text is truncated with an overflow marker (`…`), and sub-collections (select options, action rows, buttons per row) are clamped to their caps. Nothing ever exceeds a platform limit. The limits ship as `DISCORD_LIMITS` (see [DISCORD_LIMITS](/reference/bot/discord/DISCORD_LIMITS)).

## Related

- [discord()](/reference/bot/discord) — the adapter that calls this on every post
- [BotNode](/reference/bot/types/BotNode) — the IR this consumes
- [renderToIR](/reference/bot/functions/renderToIR) — producing the IR from JSX
- [DISCORD_LIMITS](/reference/bot/discord/DISCORD_LIMITS) — the per-element budget table
