{
  "$comment": [
    "QUARANTINE MANIFEST for the shell-docs unit suite's CI gate.",
    "",
    "EMPTY, and that is the goal state. Every shell-docs unit test is inside the",
    "CI gate. When the gate first landed the suite was red with twelve failures",
    "in six files; all twelve are now fixed.",
    "",
    "TO ADD an entry you must be able to state the reason and the exit criterion.",
    "`scripts/quarantine-ratchet.ts` then runs each quarantined file in its OWN",
    "vitest invocation and requires it to STILL FAIL, so an entry can never",
    "outlive the failure it excuses.",
    "",
    "`file` paths are relative to `showcase/shell-docs/` and must match the path",
    "vitest reports, so the exclude glob and the ratchet agree."
  ],
  "quarantined": []
}
