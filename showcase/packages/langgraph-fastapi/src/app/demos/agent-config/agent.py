"""
Agent implementation for Agent Config Object

The real graph lives at `src/agents/src/agent_config_agent.py` and is
registered in `langgraph.json` under the `agent_config_agent` id. This
placeholder exists so the per-demo folder matches the canonical layout
used by the other fastapi demos.
"""
