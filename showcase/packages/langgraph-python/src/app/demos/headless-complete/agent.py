"""
Agent implementation for Fully Headless UI.

This demo uses the same LangGraph runtime graph as the other showcase demos.
The frontend is the feature under test: it renders the chat shell headlessly and
resolves registered tool renderers with useToolRenderingResolver.
"""
