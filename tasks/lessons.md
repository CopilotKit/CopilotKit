# Lessons

- When the user states an explicit implementation goal, treat that goal as the
  authority and proceed. Internal design or planning artifacts support the
  work; they are not additional product decisions or approval gates unless the
  user asks for that workflow.
