---
title: "Open Generative UI"
icon: "lucide/Code"
description: "Let agents generate fully interactive HTML/CSS/JS UIs that stream live into the chat."
hideTOC: true
---
import { IntegrationGrid } from "@/components/content/integration-grid";

## What is this?

Open Generative UI lets the agent generate complete, sandboxed UI on the fly — HTML, CSS, and JavaScript — and stream it live into the chat. Instead of choosing from predefined components, the agent creates any UI it needs on demand.

## Choose your AI backend

<IntegrationGrid path="generative-ui/open-generative-ui" />
