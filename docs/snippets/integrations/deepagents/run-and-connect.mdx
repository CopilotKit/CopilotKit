import { Tabs, Tab } from "fumadocs-ui/components/tabs";

You'll need to run your agent and connect it to CopilotKit before proceeding.

If you don't already have CopilotKit and your agent connected, you can follow the instructions in the [quickstart](/deepagents/quickstart) guide.
